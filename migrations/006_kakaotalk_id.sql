-- 카카오톡 아이디를 받는다. 선택이다.
--
-- `kakao_id`와 다른 값이다. 그건 카카오 로그인이 앱마다 다르게 발급하는 회원번호라
-- 카카오톡 친구찾기에 넣을 수 없다. 연결은 운영자가 카톡에서 두 사람을 직접 찾아
-- 단톡방을 만드는 방식이므로(PRODUCT 45), 찾을 수 있는 값이 있어야 한다.
--
-- 필수로 하지 않는다. 카카오톡 아이디는 기본으로 있는 값이 아니라 설정에서 직접
-- 만들어야 하고, 가입 화면에서 그걸 요구하면 거기서 이탈한다.
-- 비어 있으면 지금까지처럼 닉네임으로 찾는다.

SET time_zone = '+00:00';

ALTER TABLE `user`
  ADD COLUMN kakaotalk_id VARCHAR(50) NULL AFTER kakao_id;
